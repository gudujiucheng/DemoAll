include ':app', ':algorithm', ':myview', ':designpattern', ':libnet', ':javatest', ':rxjava', ':other', ':sample', ':base', ':debug-dialog', ':aidlservice'
setBinding(new Binding([gradle: this]))
evaluate(new File(
        settingsDir,    //若做为原应用的子mudule，则为settingsDir
        'my_flutter/.android/include_flutter.groovy'
))
