<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.canzhang.sample">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- 自定义权限 -->
    <uses-permission android:name="zc.permission.DANGEROUS_TEST" />
    <uses-permission android:name="zc.permission.NORMAL_TEST" />

    <application
        android:name=".debug.DebugBaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/sample_app_name"
        android:supportsRtl="true"
        tools:replace="android:allowBackup,android:icon,android:label">

        <!-- android:alwaysRetainTaskState="true" -->
        <activity
            android:name=".ComponentListActivity"
            android:alwaysRetainTaskState="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:host="app"
                    android:scheme="fenqile" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CommonComponentSampleActivity"
            android:configChanges="keyboardHidden|orientation|screenSize" />
        <activity android:name=".manager.qrcode.QRCodeActivity" />
        <activity android:name=".manager.weex.WeexActivity" />
        <activity android:name=".manager.fragment_test.ContainerActivity" />
        <activity
            android:name=".manager.activity_test.TranslateActivity"
            android:theme="@android:style/Theme.Translucent" />
        <activity android:name=".manager.flutter_test.FlutterTestActivity" />
        <activity android:name=".manager.activity_test.task.StandardActivity" />
        <activity
            android:name=".manager.activity_test.task.SingleInstanceActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".manager.activity_test.task.SingleTaskActivity"
            android:launchMode="singleTask" />
    </application>

</manifest>