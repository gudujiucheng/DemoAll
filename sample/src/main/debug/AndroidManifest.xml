<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.canzhang.sample">

    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" /> <!-- android 11适配  targetSdkVersion:30/> -->
    <!-- 自定义权限 -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    <uses-permission android:name="zc.permission.DANGEROUS_TEST" />
    <uses-permission android:name="zc.permission.NORMAL_TEST" /> <!-- 生成华为推送角标  可选项 -->
    <uses-permission android:name="com.huawei.android.launcher.permission.CHANGE_BADGE " />

    <application
        android:name=".debug.DebugBaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/sample_app_name"
        android:hardwareAccelerated="true"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        tools:replace="android:allowBackup,android:icon,android:label"
        tools:targetApi="q">
        <activity android:name=".manager.statusbar.FullscreenActivity" />
        <!-- 如果 targetSdkVersion >= 29 上，还需要在清单文件中加上 android:requestLegacyExternalStorage="true" -->
        <activity android:name=".manager.sockettest.SocketTestActivity" />
        <activity android:name=".manager.kotlin.KotlinTestActivity" />
        <activity android:name=".manager.jetpack.livedata.LiveDataTestActivity" />
        <activity android:name=".manager.jetpack.lifecycle.GoogleLifeCycleTestActivity" />
        <activity android:name=".manager.jetpack.lifecycle.RXLifeTestActivity" />
        <activity android:name=".manager.zhujie.BindTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <!-- 通过自定义 data 块内容指定您的完整 scheme，按照您的配置，将会组成形如"语义名://主机名/路径名"   canscheme://com.can.push/my_push_test 的 url 标识 -->
                <!-- 为防止和其他应用的跳转目标页面冲突，您可以使用带有 app 名称、app 包名等可以唯一标记应用的字段进行配置 -->
                <data
                    android:host="com.can.push"
                    android:path="/my_push_test"
                    android:scheme="canscheme" />
            </intent-filter>
        </activity>

        <receiver android:name=".manager.push.MessageReceiver">
            <intent-filter>
                <action android:name="com.tencent.android.xg.vip.action.PUSH_MESSAGE" />
                <action android:name="com.tencent.android.xg.vip.action.FEEDBACK" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".ComponentListActivity"

            android:alwaysRetainTaskState="true"
            android:exported="true">
<!--            android:theme="@style/Theme.LAUNCH_THEME"-->
            <!--        autoVerify="true"    这段代码是通知Android系统，本app在安装时需要对于安全host进行验证，验证后拉起app就不需要弹框安全提示，而是直接跳转app了-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />


                <data android:scheme="https" />
            </intent-filter>

        </activity> <!-- android:alwaysRetainTaskState="true" -->
        <activity
            android:name=".CommonComponentSampleActivity"
            android:configChanges="keyboardHidden|orientation|screenSize" />
        <activity android:name=".manager.qrcode.QRCodeActivity" />
        <activity android:name=".manager.weex.WeexActivity" />
        <activity android:name=".manager.fragment_test.ContainerActivity" />
        <activity
            android:name=".manager.activity_test.TranslateActivity"
            android:theme="@android:style/Theme.Translucent" />
        <activity android:name=".manager.flutter_test.FlutterTestActivity" />
        <activity android:name=".manager.activity_test.task.StandardActivity" />
        <activity
            android:name=".manager.activity_test.task.SingleInstanceActivity"
            android:launchMode="singleInstance" />
        <activity
            android:name=".manager.activity_test.task.SingleTaskActivity"
            android:launchMode="singleTask" />
        <activity android:name=".manager.behavior.BehaviorTestActivity" />
        <activity android:name=".manager.antifraud.notification.NotificationTestActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.canzhang.sample.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/sample_file_paths" />
        </provider>

        <service
            android:name=".manager.behavior.service.BehaviorService"
            android:exported="false" />
        <service
            android:name=".manager.antifraud.notification.NotificationCollectorService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name="com.canzhang.sample.manager.log.service.LogInfoService"
            android:enabled="true"
            android:exported="true"
            android:process="mati.log.hook"/>
    </application>

</manifest>